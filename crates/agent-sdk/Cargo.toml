[package]
name = "agent-sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
schemars = { version = "0.8", features = ["derive"] }

[dev-dependencies]
serde_yaml = "0.9"
